# DESIGN.md — moved to local storage

This file is no longer maintained in the repository.

The living design document is kept locally on the project owner's machine
and is read/updated by Claude at the start of each session from the local
project folder. It is not committed to GitHub to avoid the accidental
double-encoding issues that arose when Claude's GitHub MCP committed the
raw API response instead of the decoded content.
